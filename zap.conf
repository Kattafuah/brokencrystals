# Define target URL and scan settings
target=https://localhost:8080  # Adjust this to match your application’s URL

# Authentication (if required for your application)
# auth_loginUrl=https://localhost:8080/login
# auth_username=user
# auth_password=pass

# Define spider and scan options
spider.maxDepth=5
spider.maxChildren=10
scan.policy=Default Policy  # Choose a custom policy if required

# Report settings
report.format=html  # Options: html, xml, json
report.output=zap_report.html
